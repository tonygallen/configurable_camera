( appsrc name=source is-live=true block=false format=time caps=video/x-raw,format=RGB,width=%d,height=%d,framerate=%d/1 !
 tee name=t ! queue name=raw_queue ! splitmuxsink name=sink muxer=identity location=/dev/null max-size-time=600000000000 t. !
 queue name=rtsp_queue max-size-buffers=2 max-size-time=0 max-size-bytes=0 leaky=downstream ! videoconvert ! video/x-raw,format=I420 ! nvvidconv name=converter ! video/x-raw(memory:NVMM),format=I420,framerate=%d/1 !
 nvv4l2h264enc bitrate=20000000 profile=0 preset-level=2 maxperf-enable=true ! h264parse ! video/x-h264,alignment=au,stream-format=byte-stream ! rtph264pay name=pay0 )

______________

This file configures the GStreamer pipeline for event camera sources (e.g. DVXplorer, EVK4).
The appsrc element is fed raw RGB frames by the faery frame feeder subprocess.

The pipeline description spans four lines of text, following the same structure as pipeline.txt:
 - To create the combined rtsp/raw pipeline, the app reads and concatenates all four lines.
 - To create the rtsp-only pipeline, the app skips the second line (tee and raw recording branch).
 - All content related to the raw branch and tee structure is contained to the second line.
 - Each line except the fourth ends with "!" and each line except the first starts with " "
   to link it to the next element on the next line.
 - The %d format placeholders are filled in with the event camera width, height, and frame rate.
   The four values (in order) are: width, height, framerate (appsrc caps), framerate (converter caps).

Latency tuning:
 - rtsp_queue uses max-size-buffers=2 leaky=downstream so the queue drops old frames rather than
   buffering up to the GStreamer default of 1 second (30 frames at 30 fps).
 - profile=0 selects H.264 Baseline, which has no B-frames and no look-ahead delay.
   (profile=4/High allows B-frames, adding multi-frame encoder latency.)
 - The remaining latency comes from the RTSP client jitter buffer (VLC default 1000 ms);
   use --network-caching=200 to reduce it: vlc --network-caching=200 rtsp://...

Note: this pipeline DOES need videoconvert before nvvidconv, same as the test script.
nvvidconv on Jetson cannot accept RGB directly from system-memory buffers — regardless of
whether those buffers come from appsrc or fdsrc.  The appsrc caps describe the format so
downstream elements are correctly negotiated, but nvvidconv still requires I420 system-memory
input.  videoconvert handles the CPU-side RGB→I420 conversion; nvvidconv then copies the
I420 frames from system memory into NVMM memory for the hardware encoder.
The standalone test script (scripts/test_event_camera_feeder.sh) also needs rawvideoparse
before videoconvert, because fdsrc produces raw bytes with no GStreamer buffer metadata
(appsrc does not need rawvideoparse for the same reason — it has explicit caps set).

The function gst_pipeline_txt_gen, implemented in src/config.c, is responsible for reading this file.
